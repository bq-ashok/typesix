import { MouseEventDirective } from './mouse-event.directive';
describe('MouseEventDirective', () => {
  it('should create an instance', () => {
    const directive = MouseEventDirective;
    expect(directive).toBeTruthy();
  });
});
